# Tech Stack Rules

- Frontend: Vanilla JS, D3.js (maps), Chart.js (charts) in single index.html
- Data pipeline: Python 3 scripts in scripts/
- Data format: JSON files in data/
- Map data: US Atlas TopoJSON via CDN
- CSS: Custom, dark theme, responsive
- No build system or bundler - everything runs directly
- Python deps in requirements.txt
